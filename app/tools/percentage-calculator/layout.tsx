import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Percentage Calculator - Calculate Percentages Online | DevToolkit',
  description: 'Free online percentage calculator. Calculate percentages quickly and easily.',
  keywords: 'percentage calculator, calculate percentage, percent calculator, percentage',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
