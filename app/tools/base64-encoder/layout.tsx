import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Base64 Encoder/Decoder - Encode & Decode Online | DevToolkit',
  description: 'Free Base64 encoder and decoder. Convert text to Base64 and decode Base64 strings instantly.',
  keywords: 'base64 encoder, base64 decoder, encode base64, decode base64 online',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
